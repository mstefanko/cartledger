# VCS
.git
.gitignore
.gitattributes

# Build / dependency output
node_modules
web/node_modules
web/dist
dist
build

# Runtime state (never bake into image)
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Local env / secrets
.env
.env.*
!.env.example

# Editor / OS cruft
.vscode
.idea
.DS_Store
*.swp

# Planning + docs (gitignored per CLAUDE.md; also no need in image)
PLAN-*.md
ANALYSIS-*.md
*.md
!README.md

# CI / local tooling
.github
Makefile
docker-compose*.yml
Dockerfile
.dockerignore

# Tests / coverage artifacts
coverage.out
*.test
